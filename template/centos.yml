spec:
  containers:
  - name: main
    image: docker.io/centos/tools:latest
    command:
    - bash
    - -c
    - 'sleep infinity'
    securityContext:
      privileged: true
  terminationGracePeriodSeconds: 3
