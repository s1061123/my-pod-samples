spec:
  containers:
  - name: main
    image: registry.fedoraproject.org/fedora:33
    command:
    - bash
    - -c
    - 'sleep infinity'
    securityContext:
      privileged: true
  terminationGracePeriodSeconds: 3
